<%= render "partials/sidebar", locals: {current_user: current_user, page: 3} %>
<%= render "partials/headerbar", locals: {current_user: current_user, page: 3} %>
<%= render "partials/chat_sidebar", locals: {current_user: current_user, page: 3} %>
<div class="content-wrap ">
    <!-- main page content. the place to put widgets in. usually consists of .row > .col-md-* > .widget.  -->
    <main id="content" class="content" role="main">
        <ol class="breadcrumb">
            <li>Home</li>
            <li class="active">Post</li>
        </ol>

        <section class="widget">
            <header class="row">
                <h5 class="col-md-10"><%= @post.title %></h5>
                <p class="col-md-9"><%= @post.body %></p>
            </header>
            <div class="widget-body">
            </div>
        </section>

        <section class="widget">
            <header>
                <h4>Replies</h4>
            </header>
            <div class="widget-body">
                <div class="mt">
                    <table id="datatable-table" class="table table-striped table-hover">
                        <tbody>
                        <% @post.comments.each do |comment| %>
                          <tr>
                            <td>from: <%= comment.user.username %></td>
                            <td><%= comment.body %></td>
                          </tr>
                        <% end %>
                        </tbody>
                    </table>
                </div>
            </div>
        </section>
    </main>
</div>
<!-- The Loader. Is shown when pjax happens -->
<div class="loader-wrap hiding hide">
    <i class="fa fa-circle-o-notch fa-spin-fast"></i>
</div>